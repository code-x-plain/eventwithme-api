# Ignore everything in prod secrets by default
*

# Keep the public encryption key and encrypted secret files
!prod.encrypt.public.php
!*.php

# Never commit the private decrypt key
prod.decrypt.private.php

# Keep this .gitignore itself
!.gitignore